package repository

type (
    {{range $repository := .Repositories}}
    {{$repository.Name}}Repository interface {
    {{range $function := $repository.Functions}}
    {{$function}}(ctx context.Context, request repositoryDTO.{{$function}}RepositoryRequest) (repositoryDTO.{{$function}}RepositoryResponse, error)
    {{end}}
    }
    {{end}}
)